| # | Що знайшов | Як це проявилось би в проді | Чому так сталось | Фікс | Тест на регрес |
|---|---|---|---|---|---|
|   |   |   |   |   |   |
|1|Причину розбіжності в грошах|Виручка б стабільно росходилась на n суму, через це в бухгалетрів компанії могли бути проблеми через не правильні звіти|Через те, що стажер використав Double Precision i float для грошей, вони записують число в двійковий код з мінімальною похибкою, через це на дистанціїї буде невеличка розбіжність в виручці|У файлі schema.sql замінив тип колонки total на Numeric, в файлі etl.py замінив тип колонки total з float()  на decimal()| Провів тест через tests/inc3.py, також перевірів через make check-money
|   |   |   |   |   |   |
|2 |Таку саму розбіжність як і в інцеденті з грошима, але тепер вже з колонкою dist |При вирахуванні в загаьному скільки кілометрів було проїхано була б не точна цифра| Через ту саму причину, дистанцію поїздки записано через float() і double precision, при переводі в двійковий код знову би вимальовувалась розбіжність | Вирішив не фіксити цей інцедент, бо немає від цього сенсу, в любому випадку майже неможливо вирахувати точну дистанцію поїздки, бо водій може проїхати трошки більше або трошки менше на пару метрів, тому розбіжність яка буде викликана через те, що були використані не ті типи данних не буде така суттєва| Тести не проводились, бо не було фіксу 
|   |   |   |   |   |   |
|3| Неможливіть витягнуи і завантажити дані за минулі місяці| Було б неможливо вионувати аналітику за попередні періоди через те, що в команду python etl.py не можна було прописати дату, щоб витянути інформацію з файлу наприклад за 2024-02 чи за період часу 2024-01:2024-03. Через це скрипт міг би читати файл тільки який має в собі дату, яка була на момент його зпуску | Через те, що в файлі etl в функціїї run() було прописано, щоб закидалась інформація з файлу за дату яка була в моменті виклику команди в терміналі, через те, що зараз 2026 рік і 7 місяць, скрипт намагався зчитати файл з інформацією за липень 2026. | В файл etl.py імпортував бібліотеку argparse і прописав в функції run() умову, що тепер коли хтось захоче запустити python etl.py в терміналі, то він буде змушений дописати ще --month aбо --backfill для того щоб скрипт зрозумів за яку саме дату чи за який період часу йому потрібно взяти файли і виконати над ними дії | Перевірив через термінал, колись при запуску команди python etl.py було написано,що завантажено 0 рядків.Тепер коли я дописую наприклад --month 2024-01 або --backfill 2024-01:2024-12, то завантажуються вже всі фалйи по 500 тисяч рядків кожен. Також я перевірив це своїми тестами (tests/inc2.py) в першій функції тестуємо чи пропуститть парсер наш інпут без вказаної конкретної дати, якщо return code > 0, це означає, що парсер відпрацював правильно і не дав виконати команду python etl.py без --month чи --backfill. В другій функції перевіряємо, що буде якщо написати все правильно, python etl.py --month 2024-01, тоді наш return code буде дорівнювати 0, це означає, що наша функція успішно відпрацювала і вона дійсно правильна. Тепер проовдимо такий самий тест для python etl.py --backfill 2024-01:2024:12 , return code теж 0 - значить теж все працює.
|   |   |   |   |   |   |
|4| Вразливість до sql ін'єкції| Зловмисник міг би замаскувати sql ін'єкцію під просту інформацію яку закидають в базу данних, цей запит би виконався, наприклад замаскували б DROP TABLE trips; --, тоді б дашборд би зламався і вся інформація з бази даних теж би видалилась| Стажер через пайтон склеював і форматвав рядок і відправляв його бібліотеці psycopg2 і бд, вони бачивши вже готовий гарний рядок просто виконували його, через це якщо б в рядок була вшита sql ін'єкція, вони б без питань її виконали| Замінив % на кому між запитом і тюплом з даними, також забрав лапки навколо другого %s, бо тепер код не сам фоматується через оператори пайтона і готовий передається на виконання, а тепер ми просто передаємо 2 аргумента бібліотеці psycopg2 і вона вже сама додає 2 аргументa і форматує їх.| В папці tests зробив файл sql_injection.py, в ньому я спробував підкинути sql ін'єкцію яка намагається передати команду DROP TABLE під виглядом данних, тому цей тест перевірє чи psycopg2 зможе нейтралізувати ін'єкцію без падіння з синтаксичною помилкою.|
|   |   |   |   |   |   |
|5| Не ідемпотентний код, база данних дублюється з кожним перезавантаженням. Також дублікати в базі данних | Як і описано в 1 інцеденті, при перезавантаженні бази даних, дані би двоїлись, через це було б важко розрізнити які дані реальні, а які дублікати  | Тому що стажер написав не ідемпотентний код. Наприклад грузився якийсь файл в базу даних під час чого стався якийсь збій, черговий наприклад, перезапустив скрипт, і він знов дублював цей файл, через це файл практично потроївся, бо черговий його 3 раза перезапустив | В файлі schema.sql було додано рядок UNIQUE(vendor, pickup, dist), тепер дані не будуть повторюватись, бо шанс того, що в 2х різних поїздках буде однаковий vendor, pickup, dist практично дорівнює 0. Також в файл etl.py був доданий запит ON CONFLICT ... DO NOTHING і тепер коли бд при запиті бачить, що такий рядок вже був завантажений, вона просто ігнорує цей рядок і продовжує далі. Щоб почистити від дубілкатів бд і щоб вона дізналась про файл schema.sql я просто використав make reset і потім знову вже закинув всі фалйи(не впевнений, чи це правильне рішення, можливо треба було зробити якось по інакшому).| На сам перед перевірив через термінал, командою make check-idempotent. Також перевірив тестом (tests/inc1.py). Перевірив чи рядки не будуть двоїтися, написав скрипт який в терміналі пише запит на завантаження файлу через etl.py, а потім рахує кількість всіх даних в бд, в тій самій функції проводяться ще раз такі самі дії і в кінці порівнюється 1 розрахунок з 2 розрахунком, якщо вони рівні, тоді код 0 і тест успішно пройдений.|
|   |   |   |   |   |   |
|6|Не було окремого файлу куди б складалися всі биті, відкинуті рядки| Не можливо було б провести аналітику, чому той чи інший рядок був відкинутий і не закинутий в базу данних | Всі биті рядки закидались просто в список rejects і нікуди не виводились | В файлі etl.py трішки вдосконалив закидання проблемних рядків в список, тепер закидається рядок разом із причиною чого він відкинутий. Далі якщо список битих рядків не пустий виконуєтсья наступне : Створюється новий файл dead-letter.ndjson і кожен битий файл в списку з битими файами закидується в ново створений dead-letter.ndjson| Зробив тест(tests/dead_letter_test.py, функція test_dead_letter()) який через бібліотеку subprocess запускає термінальну команду яка проганяє через пайплайн битий файл, потім перeвіряє чи створився dead-letter.ndjson , якщо так, то перевіряє чи він не пустий, якщо не пустий, то тест пройдений і цей тестовий файл видалється |
|7| Не було такого, що якщо в файлі більше 5% битих рядків, то він не закидається | Дані за місяць залились би не повністю, але ніякої помилки ніхто б не помітив, аналітики бачиивши, що різко впала кількість поїздок, були б теж здивовані через що так сталось, а насправді це все б було через те, що файл не закинувся повнітю| Через те, що стажер впринципі не прописав цей момент в коді | в etl.py нам потрібно поділити довжину списку з битими рядками, поділиити на кількість рядків, якщо в результаті ділення число буде більше за 0.05 ми падаємо і очищаєм за собою  всі дані і також записуємо відхилені рядки в dead letter з причиою | Тест (tests/dead_letter_test.py, функція test_five_percent) в ньоум ми підключаємось до бази даних, перевірємо скільки рядків на початку, щоб потім перевірити чи нові рядки не добавляться після того як ми впадемо. В середині виконуємо команду на виконання битого файлу з даними, якщо return code == 1 тест пройдений, і перевірємо скільки рядків в кінця залишилось, якщо на початку і в кінці однаково рядків, значить очищення за собою теж працює|
|   |   |   |   |   |   |
|8| Стажер написав код який тратить забагто пам'яті | OOM killer зупинив би весь процес etl файлу або файлів в базу данних, через те, що память би вичерпалась| Через те, що код спочатку читав файли, потім робив для них окермий список, закидав їх туди, потім з цього списку роскидав файли знову між списками good i rejects, це займало дуже багато пам'яті| Зробив так, що тепер не робиться ще один лишній список на етапі читання файлу, також рядки не роскидаються в списоки rejects i good, а тепер через те що це всьо робиться через генератор, yield обробляє по одному рядку і маркує його або good або bad і видаляє цей рядок з пам'яті тим самим її економить | Написав свій тест(tests/memory_test.py) в ньому subprocess виконує команду --backfill за цілий рік в теріміналі, якщо ліміт пам'яті не був перевершений - тоді тест продйний. Також виконав тест через make check memory|
|   |   |   |   |   |   |
|9| Не було логів з run id | Не можливо було б побачити деякі помилки, наприклад, якщо запит в базу данних не пройшв успішно, або скрипт зламався ще по дорозі до того як встиг би заикнув рядок з помилкою в dead letter. Також неможливо було би провести аналітику і подивитись чи напркилад файл за новий місяць був закинутий в базу даних| Просто стажер впринципі цього не добавив | В файлі etl.py використав logging, structlog і contextvars. З самого початку я взагальному настроїв логи добавивши їм structlog.contextvars.merge_contextvars, коли в якісь функції буде викликано наприклад structlog.info, то merge_conflicts перехоплює виклик і шукає чи немає там ніяких змін і зливає обновлену інформаціюos.path.getsize() в загальний список поточного логу, потім я добавив рівень важливості логів, потім добавив точний час коли відбувається весь процес, потім після налаштувань я запустив логер. Потім в кожній функціїї викликаєтся logger він створює json який наповнює даними|Зробив простенький тест на логи(tests/los.py) subprocess виконує команду python etl.py --month 2024-05 в термінал, якщо len(result.stdout) > 0, значиь логи після виконання команди не пусті і все працює добре |
|   |   |   |   |   |   |
|10| Не були прописані exit codes: 0, 1, 2, також код був вразливий до HTTP помилок |При любій http помилці скрипт би навіть не намагався перепідключитись до серверу. Скрипт міг би впасти, із незрозумілим traceback  і через відсутність exit codes система не зрозуміла б, чи скрипт впав через помилку з підключенням до серверу чи це якась проблема з даними |Не були прописані ні exit codes ні захист від http помилок | Тепер коли скрипт не може дойти до кінця, від нього буде ясна відповідь - коди 0, 1, 2. Також якщо він наткнеться на HTTP error відповідю на помилку вискочить exit code 2. Також я добавив бібліотеки tenacity, а саме retry тепер буде повторна спроба підключення до сервера якщо вилізе одна з HTTP помилок. Це було зроблено так само як це роблять в Amazon. Повторні спроби будуть виконані максимум 3 раза з різним періодом в часі - максимум 60с | Провів тест(tests/exit_codes.py) за допомогою бібліотеки subprocess виконав команди у термінал, щоб викликати код 2 я через subprocess виконав в терміналі команду python etl.py без --month чи --backfill, якщо return code  == 2 - тест пройдений успішно. Другий тест - тест на код 1. виконую команду в термінал python etl.py --month 2024-06-broken - в цьому файлі більш ніж 5 проценжтів битих рядків, тому має видати код 1 і не закидувати в бд цей файл - якщо return code == 1 - тест успішно пройдений. Останнім тестом був тест на код 0. Так само як і в двох минулих випадках я через subprocess виконав команду в терміналі, на цей раз python etl.py --month 2024-06 , це не битий файл і в ньому вже прописаний --month тому return code буде 0 і тест буде теж пройденим. Останній тест тестує саме на HTTP error, через subprocess виконується команда на закидання фалу в бд якого просто не існує python etl.py --month 2024-13, скипт має віддати 404 Not Found, потім tenacity спробує 3 раза виконати запит до серверу, це також перевіряється в тесті ми рахуємо скільки раз логер прописав 'download_started' в result.stdout через result.stdout.count('download_started'), якщо після 3 спроб все одно не вийшло підключитись до серверу - тест успішний|  
|   |   |   |   |   |   |
|11| В requirements.txt не було бібліотеки pytest та інших | В проді - ніякх, але людині яка працювала б з пайпайном після стажера було б трошки не зручно, бо довелось би в ручну качати бібліотеки яких не було добввлено в requirements | Стажер не добавив pytest в requirements.txt | Добавив всі нові бібліотеки які я використоував pip freeze > requirements.txt| Думаю, що тест для такої зміни не потрібен |
|   |   |   |   |   |   |
|12| Не оптимізовану швидкість | Скрипт дуже довго працював би, наприклад --backfill за один рік займав би +- 20хв | Тому що стажер використоував цикл фор, брав по одному рядку, поім через INSERT цей 1 рядок закидав в базу даних, через це скрипту приходилось виконувати цю дію 500 000 раз всього лиш для 1 місяця. | Для початку я створив тимчасовий файл temp_csv в який будуть закидутивсь рядки які yield помаркував як хороші, тому що COPY може копіювати тільки з файлів в таблицю або навпаки. через writer.writerow робляться 4 колонки: vendor, pickup, dist, total потім їх записує через кому в форматі csv. Потім вже при відкритому файлі temp_csv Створюємо тимчасову табличку, потім за допомгою бібліотеки psycopg2 викликаємо cope_expert() і в середині купіюємо всі дані з файлу temp_csv в temp_trips, потім з тимчасової таблиці вже просто всі зразу хороші рядки перекдаютьс яза один INSERT в нашу вже справжню таблицю trips (Коли я оптимізував швидкість, з'явилась нова проблема, в мене почалась розбіжність в грошах, яку я так і не зміг пофіксити, якщо б на реальному проекті треба було б вибрати щоб залишити правильний підрахунок грошей чи швидшу роботу скипта, я б вибрав правильний підрахунок грошей, в цій ситуації я вирішив залишити оптимізовану швикдість, бо її було вазе зробити, чим пофіксити гроші)| Зробив тест(speed_test) в ньому зробив функцію з тестом в якій включив таймер, через subprocess виконав python etl.py --month 2024-03 якщо вона триває менше 60 секунд - вимога про швидкість виконана
|   |   |   |   |   |   |
|13| При закидданні одного файлу в бд, приходили ще якісь лишні | Була б плутанина з даними в бд | Скоріш за все такі дані попали випадково не туди через якийсь системний збій або людську помилку| Не був прописаний фільтатор, який буде відхиляти дані які попали не туди| В функції з генератором перед блоком з try прописоємо if, якщо дата рядку починається з дати якої немає в змінній target_month, то його yield промаркує як bad і його закине в dead-letter| Тест(tests/data_test.py) в ньому я запустив python etl.py --month 2024-01 через subprocess, потім через psycopg2 виконуєтсь sql запит  який перевіряє коли підібрали людину і в якому році. Потім за допомогою fetchall() витягуємо всі потрібні нам дані і записуємо в змінну. В результаті ми перевіряємо чи довжина змінної не більша за 1, якщо не більша - тест продйено